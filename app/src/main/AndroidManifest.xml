<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.svees.workit">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_workit"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_workit"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListWorkoutActivity"></activity>
        <activity android:name=".ListMeasurementActivity" />
        <activity android:name=".MeasurementActivity" />
        <activity android:name=".TreningActivity" />
        <activity android:name=".AreobikActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>